if __name__ == "__main__":
    # assign variables, calculate area
    radius = 10
    area = 3.14 * radius ** 2

    # output
    print("The area of a circle with radius {} is {} meters square.".format(str(radius), str(area)))
    # this was changed after the video as I didn't realize until double-checking the jupyter notebooks that you wanted
    # .format instead of just placing the values in the {} themselves
